{
  "buildCommand": "npm install && npm run build",
  "installCommand": "npm install --prefix ../.. && npm install",
  "crons": [
    {
      "path": "/api/cron/cleanup-notifications",
      "schedule": "0 2 * * *"
    },
    {
      "path": "/api/cron/translate-markets",
      "schedule": "0 3 * * *"
    }
  ]
}
