{
  "name": "predik-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Predik prediction markets",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy predik",
    "create-local": "graph create --node http://localhost:8020/ predik",
    "remove-local": "graph remove --node http://localhost:8020/ predik",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 predik"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.35.1"
  }
}
